<view class="page">
  <view>订单详情</view>
  <view wx:if="{{error}}">{{error}}</view>
  <view wx:if="{{order}}">
    <view>订单号：{{order.orderId}}</view>
    <view>状态：{{order.status}}</view>
  </view>
</view>
